# Meta Conversions API mirror (api/capi.ts). Agency-wide token, set on the
# Vercel project — never committed. Optional: META_TEST_EVENT_CODE to validate
# in Events Manager > Test Events (remove after validating).
META_CAPI_ACCESS_TOKEN=
